# AGENTS.md

## Cursor Cloud specific instructions

This is a **documentation-only repository** (`ATLAS_CORE - Operationsraum RING-1`). It contains a single `README.md` describing an AI agent orchestration architecture.

- **No source code, dependencies, build steps, or runnable services exist.**
- **No linting, testing, or build commands are applicable.**
- There is no `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or any other configuration file.
- The only meaningful operation is editing `README.md` or adding new Markdown documentation files.
- Git operations (commit, push) are the primary development actions.
