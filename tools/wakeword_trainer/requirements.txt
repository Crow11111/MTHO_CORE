openwakeword>=0.6.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.11.0
tflite-runtime>=2.14.0; platform_system == "Linux"
# tensorflow is needed for training on Windows usually, but let's try to keep it light if possible.
# openwakeword includes onnxruntime.
