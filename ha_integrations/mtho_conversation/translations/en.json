{
  "config": {
    "step": {
      "user": {
        "title": "Set up CORE Conversation",
        "description": "Enter CORE API URL and Bearer token (HA_WEBHOOK_TOKEN from .env).",
        "data": {
          "base_url": "CORE API URL",
          "token": "Bearer Token (HA_WEBHOOK_TOKEN)"
        }
      }
    },
    "error": {
      "cannot_connect": "CORE API unreachable. Check URL and token (HA_WEBHOOK_TOKEN in .env)."
    },
    "abort": {
      "already_configured": "CORE is already configured for this URL."
    }
  }
}
