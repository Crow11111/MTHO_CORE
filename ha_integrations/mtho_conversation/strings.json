{
  "config": {
    "step": {
      "user": {
        "title": "CORE Conversation einrichten",
        "description": "CORE API URL und Bearer-Token (HA_WEBHOOK_TOKEN aus .env) eingeben.",
        "data": {
          "base_url": "CORE API URL",
          "token": "Bearer Token (HA_WEBHOOK_TOKEN)"
        }
      }
    },
    "error": {
      "cannot_connect": "CORE API nicht erreichbar. URL und Token prüfen (HA_WEBHOOK_TOKEN in .env)."
    },
    "abort": {
      "already_configured": "CORE ist bereits für diese URL konfiguriert."
    }
  }
}
